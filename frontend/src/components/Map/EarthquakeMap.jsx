import {MapContainer,TileLayer,CircleMarker,Popup}from'react-leaflet';import 'leaflet/dist/leaflet.css';import EarthquakePopup from './EarthquakePopup';
export default function EarthquakeMap({events}){return <section className="panel map"><MapContainer center={[15,0]} zoom={2} minZoom={2}><TileLayer url="https://{s}.tile.openstreetmap.org/{z}/{x}/{y}.png" attribution="© OpenStreetMap contributors"/>{events.map(e=><CircleMarker key={e.event_id} center={[e.latitude,e.longitude]} radius={Math.max(4,(e.magnitude||0)*2)} pathOptions={{color:e.alert?'#fb7185':'#38bdf8'}}><Popup><EarthquakePopup event={e}/></Popup></CircleMarker>)}</MapContainer></section>}
